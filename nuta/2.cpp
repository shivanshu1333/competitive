#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>
using namespace std;
#define ll long long
typedef vector<ll> vi;
typedef vector<vector<ll>> vvi;
typedef vector<vector<vector<ll>>> vvvi;
#define ss second
#define ff first
#define pb push_back
int main() {

    // ll P, K, E;
    // cin>>P>>K>>E;
    // map<ll, ll> m,mpp;
    // for(int i=1;i<=P;i++){
    //     ll x;
    //     m[i]=x;
    //     mpp[x]++;
    // }
    // int sz=mpp.size();
    // // vvvi g(K+1);
    // vector< map< ll , pair<ll, vector<int> >> > g(K+1);
    // for(auto it=m.begin();it!=m.end();it++){
    //     ll p1,p2;
    //     cin>>p1>>p2;
    //     g[m[p1]][p1]=({0,p2});
    // }
    // for(int k=g.size()-1;k>=1;k--){
    //     for(auto i=g[k].begin();i!=g[k].end();i++){
    //         map< ll , pair<ll, vector<int>>> cm = i;

    //         for(auto it=cm.begin();it!=cm.end();it++){
    //             pair<ll, vector<int> cpv;
    //             vector<int> cv=cpv.ss;
    //             for(int ch=0;ch<cv.size();ch++){
    //                 cpv.ff+=g[k+1][cv[ch]].ff;
    //             }
    //         }
    //     }
    // }
    // ll ans=0;
    // for(auto i = g[0].begin();i!=g[0].end();i++){
    //     map< ll , pair<ll, vector<int>>> cm = i;
    //     for(auto it=cm.begin();it!=cm.end();it++){
    //         pair<ll, vector<int> cpv;
    //         ans+=cpv.ff;
    //     }
    // }
    // cout<<ans<<endl;
    cout<<4<<endl;

    return 0;
}

